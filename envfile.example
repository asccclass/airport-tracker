# 把這個檔案複製一份改名成 envfile，填入你自己的 TDX 憑證。
# envfile 不會被打包進版本控制，也不會外流——只在你自己的機器上讀取。

# FIDS 起降資料來源：tdx（交通部 TDX 平台，客機+貨機都有，但要金鑰）或 csv（桃園機場官方 CSV，免驗證）
FIDS_SOURCE=tdx

# 去 https://tdx.transportdata.tw 註冊會員、於【會員中心 -> 資料服務 -> API金鑰】取得
TDX_CLIENT_ID=你的Client Id
TDX_CLIENT_SECRET=你的Client Secret

# 查詢的機場 IATA 代碼，桃園機場是 TPE
TDX_AIRPORT=TPE

# OpenSky 驗證身分存取（可選，沒設定就用匿名存取，額度較低容易 429）
# 優先權比 credentials.json 高。也可以不設這兩個變數，直接把官方下載的
# credentials.json 檔案放在程式同目錄，程式會自動讀取。
# OPENSKY_CLIENT_ID=你的clientId
# OPENSKY_CLIENT_SECRET=你的clientSecret
